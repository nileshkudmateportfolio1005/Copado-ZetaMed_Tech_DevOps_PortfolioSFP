<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>VR_CO_Status_Required</fullName>
    <active>true</active>
    <description>Prevents reopening of cancelled orders, locking their status.</description>
    <errorConditionFormula>AND(
       ISPICKVAL( PRIORVALUE(Status__c), &quot;Cancelled&quot;),
       NOT( ISPICKVAL( Status__c , &quot;Cancelled&quot;) ) 
)</errorConditionFormula>
    <errorDisplayField>Status__c</errorDisplayField>
    <errorMessage>Status cannot be changed once an order is cancelled.</errorMessage>
</ValidationRule>
